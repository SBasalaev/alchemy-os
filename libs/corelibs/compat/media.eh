use "/inc/media.eh"

const play_tone = playTone;
const new_player = streamPlayer;
const `Player.new` = new_player;
const `Player.get_ctype` = `Player.getCtype`;
const `Player.set_loops` = `Player.setLoops`;
const `Player.get_duration` = `Player.getDuration`;
const `Player.get_time` = `Player.getTime`;
const `Player.set_time` = `Player.setTime`;
const get_supported_ctypes = getSupportedCtypes;
const create_player = filePlayer;
